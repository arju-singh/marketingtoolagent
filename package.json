{
  "name": "marketstack",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "author": "Arju Singh",
  "description": "MarketStack — paste a GitHub repo + domain, get a full AI marketing suite. Vite React client + Express API.",
  "scripts": {
    "install:all": "npm --prefix client install && npm --prefix server install",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client run build && npm --prefix server run typecheck",
    "smoke:pay": "node scripts/smoke-razorpay.mjs"
  }
}
